stages:
  - update-payloads

update-payloads:
  stage: update-payloads
  image: python:3.9-slim
  before_script:
    - apt-get update && apt-get install -y ansible postfix
    - ansible-galaxy collection install community.general
  script:
    - ansible-playbook playbook.yml
  only:
    - main