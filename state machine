{
  "Comment": "State machine to restart a specified ECS service and validate deployment status of an array of services",
  "StartAt": "Check Deployment Status",
  "States": {
    "Check Deployment Status": {
      "Type": "Map",
      "ItemsPath": "$.ValidationServices",
      "Iterator": {
        "StartAt": "Describe Service",
        "States": {
          "Describe Service": {
            "Type": "Task",
            "Resource": "arn:aws:states:::aws-sdk:ecs:describeServices",
            "Parameters": {
              "Cluster.$": "$.ClusterName",
              "Services": [
                "$$.Map.Item.Value"
              ]
            },
            "ResultPath": "$.ServiceStatus",
            "Next": "Check If Deployment Is In Progress"
          },
          "Check If Deployment Is In Progress": {
            "Type": "Choice",
            "Choices": [
              {
                "Variable": "$.ServiceStatus.services[0].deployments[0].status",
                "StringEquals": "PRIMARY",
                "Next": "Deployment In Progress"
              }
            ],
            "Default": "Proceed With Validation"
          },
          "Deployment In Progress": {
            "Type": "Wait",
            "Seconds": 60,
            "Next": "Describe Service"
          },
          "Proceed With Validation": {
            "Type": "Pass",
            "Result": "Service is stable",
            "Next": "Check If All Services Are Validated"
          }
        }
      },
      "ResultPath": "$.ValidationResults",
      "Next": "Check If All Services Are Validated"
    },
    "Check If All Services Are Validated": {
      "Type": "Choice",
      "Choices": [
        {
          "Variable": "$.ValidationResults",
          "IsPresent": true,
          "Next": "Restart Specified Service"
        }
      ],
      "Default": "No Services To Validate"
    },
    "Restart Specified Service": {
      "Type": "Task",
      "Resource": "arn:aws:states:::aws-sdk:ecs:updateService",
      "Parameters": {
        "Cluster.$": "$.ClusterName",
        "Service.$": "$.ServiceToRestart",
        "ForceNewDeployment": true
      },
      "Next": "Wait Before Monitoring"
    },
    "Wait Before Monitoring": {
      "Type": "Wait",
      "Seconds": 300,
      "Next": "Monitor Service"
    },
    "Monitor Service": {
      "Type": "Task",
      "Resource": "arn:aws:states:::aws-sdk:ecs:describeServices",
      "Parameters": {
        "Cluster.$": "$.ClusterName",
        "Services": [
          "$.ServiceToRestart"
        ]
      },
      "End": true
    },
    "No Services To Validate": {
      "Type": "Fail",
      "Error": "NoServicesToValidate",
      "Cause": "No services found for validation."
    }
  }
}
