{
  "Comment": "State machine to manage ECS service restarts ensuring only one service is restarted at a time",
  "StartAt": "Check Flag",
  "States": {
    "Check Flag": {
      "Type": "Choice",
      "Choices": [
        {
          "Variable": "$.ExecutionInProgress",
          "BooleanEquals": true,
          "Next": "Wait and Retry"
        }
      ],
      "Default": "Proceed Without Flag"
    },
    "Proceed Without Flag": {
      "Type": "Pass",
      "Next": "Set In-Process Flag"
    },
    "Set In-Process Flag": {
      "Type": "Pass",
      "Result": true,
      "ResultPath": "$.ExecutionInProgress",
      "Next": "Update Service"
    },
    "Update Service": {
      "Type": "Task",
      "Resource": "arn:aws:states:::aws-sdk:ecs:updateService",
      "Parameters": {
        "Cluster.$": "$.ClusterName",
        "Service.$": "$.ServiceName",
        "ForceNewDeployment": true
      },
      "Next": "Wait Before Monitoring"
    },
    "Wait Before Monitoring": {
      "Type": "Wait",
      "Seconds": 300,  // 5 minutes wait
      "Next": "Monitor Service"
    },
    "Monitor Service": {
      "Type": "Task",
      "Resource": "arn:aws:states:::aws-sdk:ecs:describeServices",
      "Parameters": {
        "Cluster.$": "$.ClusterName",
        "Services": [
          "$.ServiceName"
        ]
      },
      "Next": "Clear In-Process Flag"
    },
    "Clear In-Process Flag": {
      "Type": "Pass",
      "Result": false,
      "ResultPath": "$.ExecutionInProgress",
      "End": true
    },
    "Wait and Retry": {
      "Type": "Wait",
      "Seconds": 30,
      "Next": "Check Flag"
    }
  }
}
